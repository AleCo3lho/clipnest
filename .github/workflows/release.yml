name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'

    - name: Build macOS amd64
      run: |
        GOOS=darwin GOARCH=amd64 go build -v -o clipnest-darwin-amd64 ./cmd/clipnest
        GOOS=darwin GOARCH=amd64 go build -v -o clipnestd-darwin-amd64 ./cmd/clipnestd

    - name: Build macOS arm64
      run: |
        GOOS=darwin GOARCH=arm64 go build -v -o clipnest-darwin-arm64 ./cmd/clipnest
        GOOS=darwin GOARCH=arm64 go build -v -o clipnestd-darwin-arm64 ./cmd/clipnestd

    - name: Build Linux amd64
      run: |
        GOOS=linux GOARCH=amd64 go build -v -o clipnest-linux-amd64 ./cmd/clipnest
        GOOS=linux GOARCH=amd64 go build -v -o clipnestd-linux-amd64 ./cmd/clipnestd

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          clipnest-darwin-amd64
          clipnestd-darwin-amd64
          clipnest-darwin-arm64
          clipnestd-darwin-arm64
          clipnest-linux-amd64
          clipnestd-linux-amd64
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
